@keyframes animBtnSubmitActive {
    0% {
        transform: translateY(-5px);
    }

    50% {
        transform: translateY(0) scale(1.05);
    }

    100% {
        transform: translateY(0) scale(0.97);
    }
}
